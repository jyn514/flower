Flower's docs are themselves a flower site.
You can read them directly as source files, but the intended way to view them is through flower.
To build them, run `flower watch`, then press 'o' to open http://localhost:8090/ in your browser.
